---
phase: 08-e2e-page-tests
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/__tests__/e2e/pages/TournamentDetailPage.ts
  - apps/web/__tests__/e2e/pages/TournamentRegistrationsAdminPage.ts
  - apps/web/__tests__/e2e/tournaments.spec.ts
  - apps/web/__tests__/e2e/registrations-admin.spec.ts
autonomous: true
requirements:
  - E2E-03
  - E2E-04
user_setup: []
must_haves:
  truths:
    - "Tournament detail page displays tournament info, events, and registration status"
    - "Tournament detail allows registration/unregistration"
    - "Tournament registrations admin page lists all registrations"
    - "Admin can approve/reject/manage registrations"
  artifacts:
    - path: "apps/web/__tests__/e2e/pages/TournamentDetailPage.ts"
      provides: "Tournament detail page selectors and actions"
      exports: ["TournamentDetailPage class"]
    - path: "apps/web/__tests__/e2e/pages/TournamentRegistrationsAdminPage.ts"
      provides: "Registrations admin page selectors and actions"
      exports: ["TournamentRegistrationsAdminPage class"]
    - path: "apps/web/__tests__/e2e/registrations-admin.spec.ts"
      provides: "Registrations admin E2E tests"
      tests: ["list registrations", "approve/reject", "filter by status"]
  key_links:
    - from: "registrations-admin.spec.ts"
      to: "pages/TournamentRegistrationsAdminPage.ts"
      via: "import"
      pattern: "import.*TournamentRegistrationsAdminPage"
---

<objective>
Create Tournament detail page tests and registrations admin page tests.

Purpose: Verify tournament viewing and admin registration management works.
Output: TournamentDetailPage class + expanded tournaments.spec.ts + new registrations-admin.spec.ts.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@apps/web/__tests__/e2e/tournaments.spec.ts
@apps/web/__tests__/e2e/utils/mockData.ts
@apps/web/__tests__/e2e/utils/auth.ts

# Routes for pages:
- Tournament detail: /tournaments/[id]
- Registrations admin: /tournaments/[id]/admin/registrations
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TournamentDetailPage class</name>
  <files>apps/web/__tests__/e2e/pages/TournamentDetailPage.ts</files>
  <action>
Create TournamentDetailPage class extending BasePage:
- Locators: tournamentName, tournamentSlug, eventList, registerButton, unregisterButton
- Locators: registrationStatus, venueInfo, scheduleInfo, bracketLink
- Locators: events section with event cards
- goto(tournamentId) method: navigates to /tournaments/{id}
- Methods: clickRegister(), clickUnregister(), getEventCards()
- Use semantic locators (getByRole, getByLabel, getByText)
  </action>
  <verify>File exists with TournamentDetailPage class exported</verify>
  <done>TournamentDetailPage class with locators and navigation methods</done>
</task>

<task type="auto">
  <name>Task 2: Expand Tournament detail tests</name>
  <files>apps/web/__tests__/e2e/tournaments.spec.ts</files>
  <action>
Expand existing tournaments.spec.ts with comprehensive tests:
- Use TournamentDetailPage from pages/TournamentDetailPage.ts
- Mock /api/tournaments/{id} endpoint
- Test: page loads and displays tournament name
- Test: displays tournament slug and venue
- Test: shows events list with game names
- Test: registration status displayed correctly
- Test: register button works (player not registered)
- Test: unregister button works (player registered)
- Test: 404 for non-existent tournament (already exists, expand)
- Test: error handling when API fails
  </action>
  <verify>npm run docker:test:e2e -- --grep "Tournament Flow"</verify>
  <done>Tournament detail page comprehensive tests</done>
</task>

<task type="auto">
  <name>Task 3: Create TournamentRegistrationsAdminPage class and tests</name>
  <files>apps/web/__tests__/e2e/pages/TournamentRegistrationsAdminPage.ts, apps/web/__tests__/e2e/registrations-admin.spec.ts</files>
  <action>
Create TournamentRegistrationsAdminPage class:
- Locators: pageTitle, registrationTable, registrationRows, filterDropdown
- Locators: approveButton, rejectButton, statusBadge
- goto(tournamentId) method: navigates to /tournaments/{id}/admin/registrations
- Methods: getRegistration(user), approveRegistration(user), rejectRegistration(user)

Create registrations-admin.spec.ts:
- Use asAdmin fixture for admin role
- Mock /api/tournaments/{id}/registrations endpoint
- Test: page loads with registrations list
- Test: each registration shows user info and status
- Test: filter by status (pending, confirmed, cancelled)
- Test: approve registration updates status
- Test: reject registration updates status
- Test: unauthorized access blocked for non-admin
  </action>
  <verify>npm run docker:test:e2e -- --grep "Registrations"</verify>
  <done>Registrations admin page class and comprehensive tests</done>
</task>

</tasks>

<verification>
All 3 tasks complete:
1. TournamentDetailPage class created
2. tournaments.spec.ts expanded with 8+ tests
3. TournamentRegistrationsAdminPage class + registrations-admin.spec.ts with 6+ tests
</verification>

<success_criteria>
- Tournament detail page E2E tests pass (E2E-03)
- Tournament registrations admin page E2E tests pass (E2E-04)
- All tests use POM pattern
- Tests cover admin vs player role differences
</success_criteria>

<output>
After completion, create `.planning/phases/08-e2e-page-tests/08-03-SUMMARY.md`
</output>
