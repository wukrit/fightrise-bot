---
phase: 08-e2e-page-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/__tests__/e2e/pages/BasePage.ts
  - apps/web/__tests__/e2e/pages/DashboardPage.ts
  - apps/web/__tests__/e2e/pages/TournamentListPage.ts
  - apps/web/__tests__/e2e/utils/fixtures.ts
  - apps/web/__tests__/e2e/utils/mockData.ts
autonomous: true
requirements:
  - E2E-01
  - E2E-02
user_setup: []
must_haves:
  truths:
    - "Dashboard page renders and displays user information"
    - "Tournament list page displays available tournaments"
    - "Navigation between pages works correctly"
  artifacts:
    - path: "apps/web/__tests__/e2e/pages/BasePage.ts"
      provides: "Common navigation and element selection methods"
      exports: ["BasePage class"]
    - path: "apps/web/__tests__/e2e/pages/DashboardPage.ts"
      provides: "Dashboard page selectors and actions"
      exports: ["DashboardPage class"]
    - path: "apps/web/__tests__/e2e/pages/TournamentListPage.ts"
      provides: "Tournament list page selectors and actions"
      exports: ["TournamentListPage class"]
    - path: "apps/web/__tests__/e2e/utils/fixtures.ts"
      provides: "Role-based test fixtures (asAdmin, asPlayer)"
      exports: ["test with asAdmin, asPlayer"]
    - path: "apps/web/__tests__/e2e/utils/mockData.ts"
      provides: "Factory functions for test data"
      exports: ["createTournament, createUser, createMatch"]
  key_links:
    - from: "fixtures.ts"
      to: "utils/auth.ts"
      via: "setupAuthenticatedState"
      pattern: "import.*auth"
    - from: "DashboardPage.ts"
      to: "BasePage.ts"
      via: "extends"
      pattern: "extends BasePage"
---

<objective>
Create Page Object Model infrastructure for E2E tests with base page class, role-based fixtures, and mock data utilities.

Purpose: Establish reusable patterns that all subsequent test plans will use.
Output: BasePage, DashboardPage, TournamentListPage classes + fixtures + mockData utilities.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-e2e-page-tests/08-CONTEXT.md
@.planning/phases/08-e2e-page-tests/08-RESEARCH.md
@apps/web/__tests__/e2e/utils/auth.ts
@apps/web/__tests__/e2e/dashboard.spec.ts

# Interface contracts from existing auth utilities:

From apps/web/__tests__/e2e/utils/auth.ts:
```typescript
export interface MockSession {
  user: {
    id: string;
    discordId: string;
    discordUsername: string;
    discordAvatar: string | null;
    name?: string | null;
    email?: string | null;
    image?: string | null;
  };
  expires: string;
}
export function createMockSession(userOverrides?: Partial<MockSession['user']>): MockSession;
export function mockAuthEndpoints(page: Page, options?: {...}): Promise<void>;
export function setupAuthenticatedState(page: Page, session?: MockSession): Promise<void>;
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BasePage class</name>
  <files>apps/web/__tests__/e2e/pages/BasePage.ts</files>
  <action>
Create BasePage class with common navigation and element selection methods:
- Constructor accepting Page
- goto(path: string) method for navigation
- waitForLoad() method using networkidle
- Protected helpers: getByRole, getByLabel, getByText, locator
- Helper to check if element exists
- Use semantic locators per CONTEXT.md decisions
  </action>
  <verify>File exists with BasePage class exported</verify>
  <done>BasePage class created with navigation and selector methods</done>
</task>

<task type="auto">
  <name>Task 2: Create DashboardPage and TournamentListPage classes</name>
  <files>apps/web/__tests__/e2e/pages/DashboardPage.ts, apps/web/__tests__/e2e/pages/TournamentListPage.ts</files>
  <action>
Create DashboardPage class:
- Extends BasePage
- Locators: welcomeMessage, tournamentsList, matchesList, recentActivity
- goto() method navigating to /dashboard
- Helper methods: hasWelcomeMessage(), getTournamentCards()

Create TournamentListPage class:
- Extends BasePage
- Locators: pageTitle, tournamentCards, createTournamentButton, emptyState
- goto() method navigating to /tournaments
- Helper methods: getTournamentCard(name), clickCreateTournament()
  </action>
  <verify>Both page classes exported and can be instantiated</verify>
  <done>DashboardPage and TournamentListPage classes with locators and methods</done>
</task>

<task type="auto">
  <name>Task 3: Create role-based fixtures and mock data utilities</name>
  <files>apps/web/__tests__/e2e/utils/fixtures.ts, apps/web/__tests__/e2e/utils/mockData.ts</files>
  <action>
Create fixtures.ts with role-based test fixtures:
- Extend Playwright test with asAdmin and asPlayer fixtures
- Use createMockSession from auth.ts
- Test fixtures for: regular user, admin user, tournament admin
- Export expect from playwright/test

Create mockData.ts with factory functions:
- createTournament(overrides?) - returns tournament object
- createUser(overrides?) - returns user object
- createMatch(overrides?) - returns match object
- createEvent(overrides?) - returns event object
- createRegistration(overrides?) - returns registration object
- All factory functions use uuid for unique IDs
  </action>
  <verify>Fixtures can be imported, mockData factories generate valid objects</verify>
  <done>Role-based fixtures and factory functions created</done>
</task>

</tasks>

<verification>
All 3 tasks complete:
1. BasePage class has navigation and selector methods
2. DashboardPage and TournamentListPage extend BasePage with page-specific locators
3. Fixtures provide asAdmin/asPlayer, mockData provides factory functions
</verification>

<success_criteria>
- BasePage, DashboardPage, TournamentListPage classes created
- Fixtures for role-based testing (asAdmin, asPlayer) created
- Mock data factory functions (createTournament, createUser, etc.) created
- All files use TypeScript with proper exports
</success_criteria>

<output>
After completion, create `.planning/phases/08-e2e-page-tests/08-01-SUMMARY.md`
</output>
