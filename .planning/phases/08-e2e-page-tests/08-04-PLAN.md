---
phase: 08-e2e-page-tests
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/__tests__/e2e/pages/TournamentMatchesAdminPage.ts
  - apps/web/__tests__/e2e/pages/AuditLogPage.ts
  - apps/web/__tests__/e2e/matches-admin.spec.ts
  - apps/web/__tests__/e2e/audit-log.spec.ts
autonomous: true
requirements:
  - E2E-05
  - E2E-06
user_setup: []
must_haves:
  truths:
    - "Tournament matches admin page lists all matches"
    - "Admin can view match details and update scores"
    - "Audit log page displays admin actions history"
    - "Audit log can be filtered by action type and date"
  artifacts:
    - path: "apps/web/__tests__/e2e/pages/TournamentMatchesAdminPage.ts"
      provides: "Matches admin page selectors and actions"
      exports: ["TournamentMatchesAdminPage class"]
    - path: "apps/web/__tests__/e2e/pages/AuditLogPage.ts"
      provides: "Audit log page selectors and actions"
      exports: ["AuditLogPage class"]
    - path: "apps/web/__tests__/e2e/matches-admin.spec.ts"
      provides: "Matches admin E2E tests"
      tests: ["list matches", "view details", "update score"]
    - path: "apps/web/__tests__/e2e/audit-log.spec.ts"
      provides: "Audit log E2E tests"
      tests: ["list actions", "filter by type", "filter by date"]
  key_links:
    - from: "matches-admin.spec.ts"
      to: "pages/TournamentMatchesAdminPage.ts"
      via: "import"
    - from: "audit-log.spec.ts"
      to: "pages/AuditLogPage.ts"
      via: "import"
---

<objective>
Create Tournament matches admin page tests and Admin audit log page tests.

Purpose: Verify tournament match management and admin action tracking work.
Output: TournamentMatchesAdminPage class + AuditLogPage class + two new test files.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@apps/web/__tests__/e2e/utils/mockData.ts
@apps/web/__tests__/e2e/utils/fixtures.ts

# Routes for pages:
- Matches admin: /tournaments/[id]/admin/matches
- Audit log: /tournaments/[id]/admin/audit
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TournamentMatchesAdminPage class</name>
  <files>apps/web/__tests__/e2e/pages/TournamentMatchesAdminPage.ts</files>
  <action>
Create TournamentMatchesAdminPage class extending BasePage:
- Locators: pageTitle, matchesTable, matchRows, roundDropdown
- Locators: player1Column, player2Column, scoreColumn, statusColumn
- Locators: viewDetailsButton, reportScoreButton, dqButton
- goto(tournamentId) method: navigates to /tournaments/{id}/admin/matches
- Methods: getMatchRow(round, position), clickViewMatch(matchId), clickReportScore(matchId)
- Use semantic locators for accessibility
  </action>
  <verify>File exists with TournamentMatchesAdminPage class exported</verify>
  <done>TournamentMatchesAdminPage class created</done>
</task>

<task type="auto">
  <name>Task 2: Create matches-admin.spec.ts</name>
  <files>apps/web/__tests__/e2e/matches-admin.spec.ts</files>
  <action>
Create matches-admin.spec.ts:
- Use asAdmin fixture for admin role
- Mock /api/tournaments/{id}/matches endpoint
- Test: page loads with matches list
- Test: matches organized by round
- Test: each match shows both players
- Test: match status displayed (pending, in-progress, completed)
- Test: view match details opens modal/page
- Test: report score button available for incomplete matches
- Test: DQ button available for admin actions
- Test: unauthorized access blocked for non-admin
  </action>
  <verify>npm run docker:test:e2e -- --grep "Matches Admin"</verify>
  <done>Matches admin page tests created</done>
</task>

<task type="auto">
  <name>Task 3: Create AuditLogPage class and audit-log.spec.ts</name>
  <files>apps/web/__tests__/e2e/pages/AuditLogPage.ts, apps/web/__tests__/e2e/audit-log.spec.ts</files>
  <action>
Create AuditLogPage class:
- Locators: pageTitle, auditTable, logRows, actionTypeFilter, dateFilter
- Locators: userColumn, actionColumn, entityColumn, timestampColumn
- goto(tournamentId) method: navigates to /tournaments/{id}/admin/audit
- Methods: filterByActionType(type), filterByDateRange(start, end), getLogEntries()

Create audit-log.spec.ts:
- Use asAdmin fixture for admin role
- Mock /api/tournaments/{id}/admin/audit endpoint
- Test: page loads with audit log
- Test: displays action type, user, entity, timestamp
- Test: filter by action type works
- Test: filter by date range works
- Test: pagination or load more works
- Test: empty state when no logs exist
- Test: unauthorized access blocked for non-admin
  </action>
  <verify>npm run docker:test:e2e -- --grep "Audit Log"</verify>
  <done>Audit log page class and tests created</done>
</task>

</tasks>

<verification>
All 3 tasks complete:
1. TournamentMatchesAdminPage class created
2. matches-admin.spec.ts with 7+ tests
3. AuditLogPage class + audit-log.spec.ts with 7+ tests
</verification>

<success_criteria>
- Tournament matches admin page E2E tests pass (E2E-05)
- Admin audit log page E2E tests pass (E2E-06)
- All tests use POM pattern
- Tests cover admin-only pages with role verification
</success_criteria>

<output>
After completion, create `.planning/phases/08-e2e-page-tests/08-04-SUMMARY.md`
</output>
