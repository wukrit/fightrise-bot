---
phase: 08-e2e-page-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/__tests__/e2e/dashboard.spec.ts
  - apps/web/__tests__/e2e/tournaments.spec.ts
  - apps/web/__tests__/e2e/tournament-list.spec.ts
autonomous: true
requirements:
  - E2E-01
  - E2E-02
user_setup: []
must_haves:
  truths:
    - "Dashboard page loads and displays user welcome"
    - "Dashboard shows user's tournaments and matches"
    - "Tournament list page displays all tournaments"
    - "Tournament list has empty state when no tournaments"
  artifacts:
    - path: "apps/web/__tests__/e2e/dashboard.spec.ts"
      provides: "Dashboard page E2E tests"
      tests: ["page loads", "displays user info", "shows tournaments"]
    - path: "apps/web/__tests__/e2e/tournament-list.spec.ts"
      provides: "Tournament list page E2E tests"
      tests: ["page loads", "displays tournaments", "empty state"]
  key_links:
    - from: "dashboard.spec.ts"
      to: "pages/DashboardPage.ts"
      via: "import"
      pattern: "import.*DashboardPage"
    - from: "tournament-list.spec.ts"
      to: "pages/TournamentListPage.ts"
      via: "import"
      pattern: "import.*TournamentListPage"
---

<objective>
Write E2E tests for Dashboard page and Tournament list page.

Purpose: Verify user-facing pages work correctly in browser with mocked auth.
Output: Expanded dashboard.spec.ts and new tournament-list.spec.ts test files.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-e2e-page-tests/08-01-SUMMARY.md
@apps/web/__tests__/e2e/dashboard.spec.ts
@apps/web/__tests__/e2e/tournaments.spec.ts

# From 08-01 (POM infrastructure):

From apps/web/__tests__/e2e/pages/DashboardPage.ts:
```typescript
class DashboardPage extends BasePage {
  readonly welcomeMessage: Locator;
  readonly tournamentsList: Locator;
  readonly matchesList: Locator;
  async goto(): Promise<void>;
  async hasWelcomeMessage(): Promise<boolean>;
}
```

From apps/web/__tests__/e2e/utils/fixtures.ts:
```typescript
export const test = base.extend<{
  asAdmin: (overrides?) => Promise<void>;
  asPlayer: (overrides?) => Promise<void>;
}>
```

From apps/web/__tests__/e2e/utils/mockData.ts:
```typescript
function createTournament(overrides?): TournamentData;
function createUser(overrides?): MockUser;
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand Dashboard page tests</name>
  <files>apps/web/__tests__/e2e/dashboard.spec.ts</files>
  <action>
Expand existing dashboard.spec.ts with comprehensive tests using POM:
- Use setupAuthenticatedState from utils/auth.ts
- Use DashboardPage from pages/DashboardPage.ts
- Test: page loads successfully
- Test: displays user welcome message with username
- Test: shows tournaments section (or empty state)
- Test: shows matches section (or empty state)
- Test: navigation to tournaments list works
- Test: handles error states gracefully
- Use role-based fixtures (asPlayer, asAdmin) to test different user types
  </action>
  <verify>npm run docker:test:e2e -- --grep "Dashboard"</verify>
  <done>Dashboard page has comprehensive tests covering loads, user info, data display, navigation</done>
</task>

<task type="auto">
  <name>Task 2: Create Tournament list page tests</name>
  <files>apps/web/__tests__/e2e/tournament-list.spec.ts</files>
  <action>
Create new tournament-list.spec.ts with full test coverage:
- Use setupAuthenticatedState from utils/auth.ts
- Use TournamentListPage from pages/TournamentListPage.ts
- Use mockData factories for tournament data
- Mock /api/tournaments endpoint
- Test: page loads successfully
- Test: displays list of tournaments with cards
- Test: tournament card shows name, date, state
- Test: click tournament navigates to detail page
- Test: empty state when no tournaments exist
- Test: error state when API fails
- Test: create tournament button exists (or is hidden for non-admins)
  </action>
  <verify>File created and npm run docker:test:e2e -- --grep "Tournament List"</verify>
  <done>Tournament list page has comprehensive tests</done>
</task>

</tasks>

<verification>
Both test files complete:
1. dashboard.spec.ts has 5+ tests covering all user scenarios
2. tournament-list.spec.ts has 5+ tests covering list, empty, error states
</verification>

<success_criteria>
- Dashboard page E2E tests pass (E2E-01)
- Tournament list page E2E tests pass (E2E-02)
- All tests use Page Object Model pattern
- Tests cover happy path and error states
</success_criteria>

<output>
After completion, create `.planning/phases/08-e2e-page-tests/08-02-SUMMARY.md`
</output>
