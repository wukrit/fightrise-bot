---
phase: 08-e2e-page-tests
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/__tests__/e2e/pages/AccountSettingsPage.ts
  - apps/web/__tests__/e2e/account.spec.ts
autonomous: true
requirements:
  - E2E-07
user_setup: []
must_haves:
  truths:
    - "Account settings page displays user profile information"
    - "User can view linked Start.gg account"
    - "User can change notification preferences"
    - "User can disconnect accounts"
  artifacts:
    - path: "apps/web/__tests__/e2e/pages/AccountSettingsPage.ts"
      provides: "Account settings page selectors and actions"
      exports: ["AccountSettingsPage class"]
    - path: "apps/web/__tests__/e2e/account.spec.ts"
      provides: "Account settings E2E tests"
      tests: ["display profile", "linked accounts", "preferences", "disconnect"]
  key_links:
    - from: "account.spec.ts"
      to: "pages/AccountSettingsPage.ts"
      via: "import"
---

<objective>
Create Account settings page tests.

Purpose: Verify user account management works correctly.
Output: AccountSettingsPage class + account.spec.ts test file.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@apps/web/__tests__/e2e/utils/mockData.ts

# Route:
- Account settings: /account
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AccountSettingsPage class</name>
  <files>apps/web/__tests__/e2e/pages/AccountSettingsPage.ts</files>
  <action>
Create AccountSettingsPage class extending BasePage:
- Locators: profileSection, discordUsername, discordAvatar
- Locators: startggSection, startggUsername, startggLinked
- Locators: linkedAccountsSection, discordConnectButton, startggConnectButton
- Locators: notificationPreferencesSection, emailNotifications, matchNotifications
- Locators: dangerZoneSection, deleteAccountButton
- goto() method: navigates to /account
- Methods: disconnectDiscord(), disconnectStartgg(), toggleEmailNotifications(), toggleMatchNotifications()
- Use semantic locators for all interactive elements
  </action>
  <verify>File exists with AccountSettingsPage class exported</verify>
  <done>AccountSettingsPage class created</done>
</task>

<task type="auto">
  <name>Task 2: Create account.spec.ts</name>
  <files>apps/web/__tests__/e2e/account.spec.ts</files>
  <action>
Create account.spec.ts:
- Use setupAuthenticatedState from utils/auth.ts
- Mock /api/user, /api/user/startgg endpoints
- Test: page loads and displays Discord profile info
- Test: displays linked Start.gg account (when linked)
- Test: displays option to link Start.gg (when not linked)
- Test: notification preferences section exists
- Test: toggle email notifications works
- Test: toggle match notifications works
- Test: shows connected/disconnected state for each service
- Test: unauthenticated users redirected to sign in
  </action>
  <verify>npm run docker:test:e2e -- --grep "Account"</verify>
  <done>Account settings page tests created</done>
</task>

</tasks>

<verification>
Both tasks complete:
1. AccountSettingsPage class created
2. account.spec.ts with 8+ tests
</verification>

<success_criteria>
- Account settings page E2E tests pass (E2E-07)
- Tests cover profile display, linked accounts, preferences
- Tests use POM pattern
</success_criteria>

<output>
After completion, create `.planning/phases/08-e2e-page-tests/08-05-SUMMARY.md`
</output>
