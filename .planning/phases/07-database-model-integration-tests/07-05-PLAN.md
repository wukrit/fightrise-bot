---
phase: 07-database-model-integration-tests
plan: 05
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
  - 07-03
files_modified:
  - packages/database/src/__tests__/models/Dispute.test.ts
  - packages/database/src/__tests__/models/GuildConfig.test.ts
  - packages/database/src/__tests__/models/AuditLog.test.ts
autonomous: true
requirements:
  - DB-07
  - DB-10
  - DB-11

must_haves:
  truths:
    - "Dispute model CRUD operations work correctly"
    - "GuildConfig model CRUD operations work correctly"
    - "AuditLog model CRUD operations work correctly"
  artifacts:
    - path: "packages/database/src/__tests__/models/Dispute.test.ts"
      provides: "Dispute model CRUD tests"
    - path: "packages/database/src/__tests__/models/GuildConfig.test.ts"
      provides: "GuildConfig model CRUD tests"
    - path: "packages/database/src/__tests__/models/AuditLog.test.ts"
      provides: "AuditLog model CRUD tests"
  key_links:
    - from: "Dispute.test.ts"
      to: "packages/database/src/__tests__/utils/seeders.ts"
      via: "createUser, createTournament, createEvent, createMatch factories"
---

<objective>
Create integration tests for Dispute, GuildConfig, and AuditLog models with CRUD operations and relationships.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@packages/database/prisma/schema.prisma
@packages/database/src/__tests__/setup.ts
@packages/database/src/__tests__/utils/seeders.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Dispute model integration tests</name>
  <files>packages/database/src/__tests__/models/Dispute.test.ts</files>
  <action>
Create Dispute.test.ts with integration tests. Tests to include:
- Create: should create dispute linked to match and initiator user
- Read: should find disputes by match, by initiator
- Update: should update dispute status, should resolve dispute with resolvedBy
- Delete: should delete dispute without affecting match
- Relationships: should verify match, initiator, and resolver relations

Use setupTestDatabase() and transaction rollback pattern. Use createUser, createTournament, createEvent, createMatch, createDispute from seeders.
  </action>
  <verify>grep -l "describe.*Dispute" packages/database/src/__tests__/models/Dispute.test.ts</verify>
  <done>Dispute.test.ts passes all CRUD tests</done>
</task>

<task type="auto">
  <name>Task 2: Create GuildConfig model integration tests</name>
  <files>packages/database/src/__tests__/models/GuildConfig.test.ts</files>
  <action>
Create GuildConfig.test.ts with integration tests. Tests to include:
- Create: should create guild config with required fields
- Read: should find guild config by discordGuildId
- Update: should update channels, should update prefix/locale/timezone
- Delete: should delete guild config

Use setupTestDatabase() and transaction rollback pattern. Use createGuildConfig from seeders.
  </action>
  <verify>grep -l "describe.*GuildConfig" packages/database/src/__tests__/models/GuildConfig.test.ts</verify>
  <done>GuildConfig.test.ts passes all CRUD tests</done>
</task>

<task type="auto">
  <name>Task 3: Create AuditLog model integration tests</name>
  <files>packages/database/src/__tests__/models/AuditLog.test.ts</files>
<action>
Create AuditLog.test.ts with integration tests. Tests to include:
- Create: should create audit log linked to user
- Read: should find logs by entity, should find logs by user
- Update: N/A - audit logs are immutable append-only
- Delete: should delete audit log without affecting user (cascade on user delete)
- Relationships: should verify user relation

Use setupTestDatabase() and transaction rollback pattern. Use createUser, createAuditLog from seeders.
  </action>
  <verify>grep -l "describe.*AuditLog" packages/database/src/__tests__/models/AuditLog.test.ts</verify>
  <done>AuditLog.test.ts passes all CRUD tests</done>
</task>

</tasks>

<verification>
Run tests: npm run docker:test (from packages/database)
</verification>

<success_criteria>
- Dispute.test.ts: 4+ tests passing
- GuildConfig.test.ts: 4+ tests passing
- AuditLog.test.ts: 4+ tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/07-database-model-integration-tests/07-05-SUMMARY.md`
</output>
