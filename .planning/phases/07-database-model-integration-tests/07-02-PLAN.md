---
phase: 07-database-model-integration-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/database/src/__tests__/models/User.test.ts
  - packages/database/src/__tests__/models/Tournament.test.ts
autonomous: true
requirements:
  - DB-01
  - DB-02

must_haves:
  truths:
    - "User model CRUD operations work correctly"
    - "Tournament model CRUD operations work correctly"
  artifacts:
    - path: "packages/database/src/__tests__/models/User.test.ts"
      provides: "User model CRUD tests"
    - path: "packages/database/src/__tests__/models/Tournament.test.ts"
      provides: "Tournament model CRUD tests"
  key_links:
    - from: "packages/database/src/__tests__/models/User.test.ts"
      to: "packages/database/prisma/schema.prisma"
      via: "Prisma client"
---

<objective>
Create integration tests for User and Tournament models with CRUD operations and basic relationships.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@packages/database/prisma/schema.prisma
@packages/database/src/__tests__/setup.ts
@packages/database/src/__tests__/utils/seeders.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create User model integration tests</name>
  <files>packages/database/src/__tests__/models/User.test.ts</files>
  <action>
Create User.test.ts with integration tests using Testcontainers and transaction rollback pattern. Tests to include:
- Create: should create user with discordId, should create linked user (discord + startgg)
- Read: should find user by discordId, should find user by startggId
- Update: should update displayName, should update startggGamerTag
- Delete: should delete user
- Relationships: should cascade delete registrations when user deleted

Follow existing setup.ts and seeders.ts patterns. Use beforeEach/afterEach for transaction rollback.
  </action>
  <verify>grep -l "describe.*User" packages/database/src/__tests__/models/User.test.ts</verify>
  <done>User.test.ts passes all CRUD tests</done>
</task>

<task type="auto">
  <name>Task 2: Create Tournament model integration tests</name>
  <files>packages/database/src/__tests__/models/Tournament.test.ts</files>
  <action>
Create Tournament.test.ts with integration tests using Testcontainers and transaction rollback pattern. Tests to include:
- Create: should create tournament with required fields, should create tournament with all settings
- Read: should find tournament by startggId, should list tournaments by state
- Update: should update tournament state, should update discord settings
- Delete: should delete tournament (cascade to events, matches)
- Relationships: should cascade delete events when tournament deleted

Follow existing setup.ts and seeders.ts patterns. Use beforeEach/afterEach for transaction rollback.
  </action>
  <verify>grep -l "describe.*Tournament" packages/database/src/__tests__/models/Tournament.test.ts</verify>
  <done>Tournament.test.ts passes all CRUD tests</done>
</task>

</tasks>

<verification>
Run tests: npm run docker:test (from packages/database)
</verification>

<success_criteria>
- User.test.ts: 5+ tests passing (create, read, update, delete, relationships)
- Tournament.test.ts: 5+ tests passing (create, read, update, delete, cascade)
</success_criteria>

<output>
After completion, create `.planning/phases/07-database-model-integration-tests/07-02-SUMMARY.md`
</output>
