---
phase: 07-database-model-integration-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/database/src/__tests__/utils/seeders.ts
autonomous: true
requirements:
  - DB-06
  - DB-07
  - DB-11

must_haves:
  truths:
    - "Tests can create GameResult records with factory function"
    - "Tests can create Dispute records with factory function"
    - "Tests can create AuditLog records with factory function"
  artifacts:
    - path: "packages/database/src/__tests__/utils/seeders.ts"
      provides: "Factory functions for test data creation"
      contains: "createGameResult, createDispute, createAuditLog"
  key_links:
    - from: "packages/database/src/__tests__/utils/seeders.ts"
      to: "packages/database/prisma/schema.prisma"
      via: "Model definitions"
---

<objective>
Add missing factory functions for GameResult, Dispute, and AuditLog models.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/database/prisma/schema.prisma
@packages/database/src/__tests__/utils/seeders.ts
@packages/database/src/__tests__/setup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add createGameResult factory</name>
  <files>packages/database/src/__tests__/utils/seeders.ts</files>
  <action>
Add createGameResult factory function to seeders.ts following existing patterns. The factory should:
- Accept prisma: PrismaClient, matchId: string, matchPlayerId: string, and optional options
- Options: gameNumber, winnerId, characterId, characterName, stageId, stageName
- Use uniqueId() for any generated IDs
- Return the created GameResult
- Import GameResult type from @prisma/client
  </action>
  <verify>grep -n "createGameResult" packages/database/src/__tests__/utils/seeders.ts</verify>
  <done>createGameResult factory exported from seeders.ts</done>
</task>

<task type="auto">
  <name>Task 2: Add createDispute factory</name>
  <files>packages/database/src/__tests__/utils/seeders.ts</files>
  <action>
Add createDispute factory function to seeders.ts following existing patterns. The factory should:
- Accept prisma: PrismaClient, matchId: string, initiatorId: string, and optional options
- Options: reason, status, resolvedById, resolution, resolvedAt
- Use uniqueId() counter internally if needed
- Return the created Dispute
- Import Dispute type from @prisma/client
  </action>
  <verify>grep -n "createDispute" packages/database/src/__tests__/utils/seeders.ts</verify>
  <done>createDispute factory exported from seeders.ts</done>
</task>

<task type="auto">
  <name>Task 3: Add createAuditLog factory</name>
  <files>packages/database/src/__tests__/utils/seeders.ts</files>
  <action>
Add createAuditLog factory function to seeders.ts following existing patterns. The factory should:
- Accept prisma: PrismaClient, userId: string, and options for action, entityType, entityId, before, after, reason, source
- Options should have sensible defaults
- Return the created AuditLog
- Import AuditAction, AuditSource types from @prisma/client
  </action>
  <verify>grep -n "createAuditLog" packages/database/src/__tests__/utils/seeders.ts</verify>
  <done>createAuditLog factory exported from seeders.ts</done>
</task>

</tasks>

<verification>
All three factory functions exist and are exported from seeders.ts
</verification>

<success_criteria>
- createGameResult(prisma, matchId, matchPlayerId, options) works
- createDispute(prisma, matchId, initiatorId, options) works
- createAuditLog(prisma, userId, options) works
</success_criteria>

<output>
After completion, create `.planning/phases/07-database-model-integration-tests/07-01-SUMMARY.md`
</output>
