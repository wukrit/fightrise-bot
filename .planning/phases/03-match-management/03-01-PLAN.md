---
phase: 03-match-management
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/api/tournaments/[id]/admin/matches/route.ts
  - apps/web/app/api/tournaments/[id]/admin/players/[playerId]/dq/route.ts
  - packages/startgg-client/src/mutations/dqEntrant.ts
autonomous: true
requirements:
  - API-06
  - DQ-01
  - DQ-02
  - DQ-03
  - DQ-04

must_haves:
  truths:
    - "Admin can fetch matches list via GET /api/tournaments/[id]/admin/matches with round/state filters"
    - "Admin can disqualify a player via POST /api/tournaments/[id]/admin/players/[id]/dq"
    - "DQ action creates audit log entry in database"
    - "DQ syncs to Start.gg via GraphQL mutation"
  artifacts:
    - path: "apps/web/app/api/tournaments/[id]/admin/matches/route.ts"
      provides: "GET matches list with filters and pagination"
      exports: ["GET"]
    - path: "apps/web/app/api/tournaments/[id]/admin/players/[playerId]/dq/route.ts"
      provides: "POST DQ player with admin authorization"
      exports: ["POST"]
    - path: "packages/startgg-client/src/mutations/dqEntrant.ts"
      provides: "Start.gg GraphQL mutation for DQ sync"
      exports: ["dqEntrant"]
  key_links:
    - from: "apps/web/app/api/tournaments/[id]/admin/players/[playerId]/dq/route.ts"
      to: "packages/startgg-client/src/mutations/dqEntrant.ts"
      via: "import and call mutation"
      pattern: "dqEntrant.*startggClient"
    - from: "apps/web/app/api/tournaments/[id]/admin/players/[playerId]/dq/route.ts"
      to: "prisma.auditLog"
      via: "prisma.$transaction"
      pattern: "auditLog.*create"
---

<objective>
Build the Admin Match Management API endpoints for listing matches and disqualifying players.

Purpose: Enable tournament admins to view matches and perform disqualifications via API, with full audit logging and Start.gg sync.

Output:
- GET /api/tournaments/[id]/admin/matches route
- POST /api/tournaments/[id]/admin/players/[playerId]/dq route
- Start.gg DQ mutation
</objective>

<context>
@.planning/phases/03-match-management/03-CONTEXT.md
@.planning/phases/03-match-management/03-RESEARCH.md
@.planning/phases/02-registration-management/02-01-SUMMARY.md
@apps/web/app/api/tournaments/[id]/admin/registrations/route.ts
@apps/bot/src/services/dqService.ts
@packages/database/prisma/schema.prisma (Match, MatchPlayer models)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Matches List API</name>
  <files>apps/web/app/api/tournaments/[id]/admin/matches/route.ts</files>
  <action>
Create GET endpoint at /api/tournaments/[id]/admin/matches with:
- Authorization via requireTournamentAdmin (use requireTournamentAdminById for server component pattern)
- Query params: state (optional), round (optional), page (default 1), limit (default 50), playerName (optional search)
- Use cache: 'no-store' for fresh data (per STATE.md)
- Include players with user data (discord username, startgg gamer tag)
- Include event with tournamentId
- Return { matches: [], pagination: { page, limit, total, totalPages } }
- Follow pattern from registrations route in 02-01-SUMMARY.md
  </action>
  <verify>npm run docker:test</verify>
  <done>GET /api/tournaments/xxx/admin/matches returns 200 with matches array and pagination</done>
</task>

<task type="auto">
  <name>Task 2: Create Admin DQ API Endpoint</name>
  <files>apps/web/app/api/tournaments/[id]/admin/players/[playerId]/dq/route.ts</files>
  <action>
Create POST endpoint at /api/tournaments/[id]/admin/players/[playerId]/dq with:
- Authorization via requireTournamentAdmin
- Accept body: { matchId, reason (optional per DQ-02) }
- Use prisma.$transaction for atomic DQ + audit log
- Create audit log with AuditAction.PLAYER_DQ, AuditSource.API
- Use state guard (updateMany with state filter) to prevent race conditions
- After successful DB update, call Start.gg mutation to sync
- Return { success: boolean, message: string }
- Handle errors gracefully with proper status codes
  </action>
  <verify>npm run docker:test</verify>
  <done>POST /api/tournaments/xxx/admin/players/xxx/dq returns 200 with success=true</done>
</task>

<task type="auto">
  <name>Task 3: Add Start.gg DQ Mutation</name>
  <files>packages/startgg-client/src/mutations/dqEntrant.ts</files>
  <action>
Create dqEntrant mutation in startgg-client:
- Use GraphQL mutation to report DQ to Start.gg
- Check if Start.gg has native DQ mutation, otherwise use reportSet with DQ flag
- Export function that takes entrantId and matchId
- Handle errors gracefully (don't fail the whole DQ if Start.gg sync fails)
- Add to client exports
  </action>
  <verify>npm run docker:test</verify>
  <done>dqEntrant function exists and can be imported from @fightrise/startgg-client</done>
</task>

</tasks>

<verification>
- [ ] GET /api/tournaments/[id]/admin/matches returns matches with proper data structure
- [ ] GET /api/tournaments/[id]/admin/matches supports state and round filters
- [ ] GET /api/tournaments/[id]/admin/matches supports pagination (50 per page)
- [ ] POST /api/tournaments/[id]/admin/players/[id]/dq returns 401 for non-admins
- [ ] POST /api/tournaments/[id]/admin/players/[id]/dq creates audit log entry
- [ ] Start.gg DQ mutation is callable
</verification>

<success_criteria>
API endpoints functional with proper authorization, filtering, pagination, audit logging, and Start.gg sync
</success_criteria>

<output>
After completion, create `.planning/phases/03-match-management/03-01-SUMMARY.md`
</output>
