---
phase: 03-match-management
plan: '02'
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/tournaments/[id]/admin/matches/page.tsx
  - apps/web/components/admin/ClientMatchesTable.tsx
  - apps/web/components/admin/MatchesTable.tsx
  - apps/web/components/admin/MatchDetail.tsx
  - apps/web/components/admin/DQModal.tsx
  - apps/web/components/admin/MatchFilters.tsx
autonomous: true
requirements:
  - MATCH-01
  - MATCH-02
  - MATCH-03
  - DQ-01
  - DQ-02

must_haves:
  truths:
    - "Admin can view matches in expandable table with filters"
    - "Admin can see match details (players, scores, check-in status) in expanded row"
    - "Admin can disqualify a player via modal confirmation"
    - "Check-in status shows color-coded icons (green check, red X, yellow clock)"
    - "Table auto-refreshes every 30 seconds"
  artifacts:
    - path: "apps/web/app/tournaments/[id]/admin/matches/page.tsx"
      provides: "Server Component - matches list page with auth"
    - path: "apps/web/components/admin/ClientMatchesTable.tsx"
      provides: "Client Component - table with auto-refresh"
    - path: "apps/web/components/admin/MatchesTable.tsx"
      provides: "Presentational - table UI with expandable rows"
    - path: "apps/web/components/admin/MatchDetail.tsx"
      provides: "Expanded row content with players, scores, check-in"
    - path: "apps/web/components/admin/DQModal.tsx"
      provides: "DQ confirmation dialog"
  key_links:
    - from: "apps/web/app/tournaments/[id]/admin/matches/page.tsx"
      to: "apps/web/app/api/tournaments/[id]/admin/matches/route.ts"
      via: "Server Component data fetching"
      pattern: "fetch.*admin/matches"
    - from: "apps/web/components/admin/ClientMatchesTable.tsx"
      to: "apps/web/app/api/tournaments/[id]/admin/matches/route.ts"
      via: "fetch for auto-refresh"
      pattern: "setInterval.*fetch"
    - from: "apps/web/components/admin/DQModal.tsx"
      to: "apps/web/app/api/tournaments/[id]/admin/players/[playerId]/dq/route.ts"
      via: "POST fetch"
      pattern: "POST.*dq"
---

<objective>
Build the Match Management UI page with matches list, expandable details, and DQ flow.

Purpose: Allow tournament admins to view matches, see detailed player information including check-in status, and disqualify players through a confirmation modal.

Output:
- Matches list page at /tournaments/[id]/admin/matches
- Client-side table with expandable rows and auto-refresh
- DQ confirmation modal
</objective>

<context>
@.planning/phases/03-match-management/03-CONTEXT.md
@.planning/phases/03-match-management/03-RESEARCH.md
@.planning/phases/02-registration-management/02-PLAN-UI-SUMMARY.md
@apps/web/app/tournaments/[id]/admin/registrations/page.tsx
@apps/web/components/admin/ClientRegistrationsTable.tsx
@packages/ui/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Matches Page Server Component</name>
  <files>apps/web/app/tournaments/[id]/admin/matches/page.tsx</files>
  <action>
Create server component page at /tournaments/[id]/admin/matches:
- Use requireTournamentAdminById for authorization
- Fetch initial matches data (use cache: 'no-store')
- Support searchParams for state, round, page filters
- Pass initial data to ClientMatchesTable
- Follow layout pattern from registrations/page.tsx (nav, header, content)
- Link to audit log in header
  </action>
  <verify>npm run docker:test</verify>
  <done>Page renders at /tournaments/[id]/admin/matches with matches data</done>
</task>

<task type="auto">
  <name>Task 2: Create Client Matches Table</name>
  <files>apps/web/components/admin/ClientMatchesTable.tsx</files>
  <action>
Create client component:
- Accept initialData and tournamentId props
- Maintain local state for matches (from initialData)
- Add auto-refresh every 30 seconds via setInterval
- Include MatchFilters component for search, round, state dropdowns
- Render MatchesTable component
- Handle DQ action - call API, show toast, refresh data
- Use Toast from @fightrise/ui for feedback
  </action>
  <verify>npm run docker:test</verify>
  <done>Table displays matches with 30-second auto-refresh</done>
</task>

<task type="auto">
  <name>Task 3: Create Matches Table UI with Expandable Rows</name>
  <files>apps/web/components/admin/MatchesTable.tsx, apps/web/components/admin/MatchDetail.tsx, apps/web/components/admin/MatchFilters.tsx</files>
  <action>
Create MatchesTable:
- Full columns: Round, Player 1, Player 2, Score, Status, Actions
- Color-coded badges: green=COMPLETED, yellow=IN_PROGRESS/DQ, red=DISPUTED
- Round labels include bracket type from roundText field
- Click row to expand inline details (not separate page per CONTEXT.md)
- Show "1/2 checked in" summary in collapsed row

Create MatchDetail (expanded content):
- Show both players with full details
- Player names: show both Discord username and Start.gg gamer tag
- Score format: game scores (e.g., "2-1")
- Check-in info: color + icon (green check, red X, yellow clock)
- Show timestamps as relative ("5 min ago") using date-fns
- DQ button in expanded section only (per CONTEXT.md)

Create MatchFilters:
- Search input for player name
- Dropdown for round filter (use Radix Select)
- Dropdown for state filter (use Radix Select)
  </action>
  <verify>npm run docker:test</verify>
  <done>Table shows all columns with expandable rows that reveal player details</done>
</task>

<task type="auto">
  <name>Task 4: Create DQ Modal</name>
  <files>apps/web/components/admin/DQModal.tsx</files>
  <action>
Create DQ confirmation modal:
- Trigger: Two-step (click player row, then confirm DQ) per CONTEXT.md
- Use Radix Dialog from packages/ui (Modal component)
- Show "Are you sure?" confirmation
- Reason input (optional per DQ-02 - CONTEXT.md says optional)
- Submit calls POST /api/tournaments/[id]/admin/players/[playerId]/dq
- Show toast feedback: "Player disqualified, syncing to Start.gg..."
- On error: show error, keep dialog open with retry option
- After success: close modal, refresh table data
  </action>
  <verify>npm run docker:test</verify>
  <done>DQ modal shows confirmation, submits to API, shows feedback</done>
</task>

</tasks>

<verification>
- [ ] Page accessible at /tournaments/[id]/admin/matches
- [ ] Matches display in table with Round, Player 1, Player 2, Score, Status columns
- [ ] Row click expands to show MatchDetail
- [ ] MatchDetail shows players, scores, check-in status with color-coded icons
- [ ] Filters work (search, round dropdown, state dropdown)
- [ ] Auto-refresh updates data every 30 seconds
- [ ] DQ button triggers modal
- [ ] DQ modal shows confirmation, accepts optional reason
- [ ] After DQ, table refreshes and shows updated state
- [ ] Unauthorized users cannot access page
</verification>

<success_criteria>
Admin can view matches in table, see detailed player info, filter by round/state/player, and disqualify players with confirmation
</success_criteria>

<output>
After completion, create `.planning/phases/03-match-management/03-02-SUMMARY.md`
</output>
