---
phase: 06-web-api-startgg-integration-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/api/tournaments/[id]/registrations/route.test.ts
  - apps/web/app/api/tournaments/[id]/matches/route.test.ts
autonomous: true
requirements:
  - API-01
  - API-02
must_haves:
  truths:
    - "Tournament registrations endpoint returns correct data with Testcontainers"
    - "Tournament matches endpoint returns correct data with Testcontainers"
    - "Both endpoints handle error cases properly"
  artifacts:
    - path: "apps/web/app/api/tournaments/[id]/registrations/route.test.ts"
      provides: "Integration tests for registrations API"
      min_lines: 50
    - path: "apps/web/app/api/tournaments/[id]/matches/route.test.ts"
      provides: "Integration tests for matches API"
      min_lines: 50
  key_links:
    - from: "apps/web/app/api/tournaments/[id]/registrations/route.test.ts"
      to: "@fightrise/database/src/__tests__/setup"
      via: "setupTestDatabase() import"
      pattern: "setupTestDatabase.*clearTestDatabase"
    - from: "apps/web/app/api/tournaments/[id]/matches/route.test.ts"
      to: "@fightrise/database/src/__tests__/utils/seeders"
      via: "createTournament.*createUser"
      pattern: "createUser.*createTournament"
---

<objective>
Create integration tests for tournament API routes (registrations and matches endpoints) using Testcontainers for real database.

Purpose: Verify API routes work correctly with a real PostgreSQL database, testing both success and error paths.

Output: Two test files in apps/web/app/api/tournaments/[id]/
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-web-api-startgg-integration-tests/06-CONTEXT.md
@.planning/phases/06-web-api-startgg-integration-tests/06-RESEARCH.md

# Key infrastructure from codebase:
- Testcontainers setup: packages/database/src/__tests__/setup.ts
- Seeders: packages/database/src/__tests__/utils/seeders.ts
- Existing API route: apps/web/app/api/tournaments/[id]/registrations/route.ts
- Existing API route: apps/web/app/api/tournaments/[id]/matches/route.ts

# Test pattern to follow:
- Import setupTestDatabase, clearTestDatabase, teardownTestDatabase from database package
- Import createUser, createTournament, createRegistration from seeders
- Use Testcontainers for database isolation
- Test both success and error responses per CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create registrations API integration test</name>
  <files>apps/web/app/api/tournaments/[id]/registrations/route.test.ts</files>
  <action>
Create integration test for GET /api/tournaments/[id]/registrations using Testcontainers:
1. Import setupTestDatabase, clearTestDatabase, teardownTestDatabase from @fightrise/database/src/__tests__/setup
2. Import createUser, createTournament, createRegistration from @fightrise/database/src/__tests__/utils/seeders
3. Create beforeAll that calls setupTestDatabase() to spin up PostgreSQL container
4. Create beforeEach that calls clearTestDatabase(prisma) for test isolation
5. Create afterAll that calls teardownTestDatabase()
6. Write test "should return registrations for tournament" - creates user + tournament + registration, calls route handler, verifies 200 with data
7. Write test "should return empty array when no registrations" - creates tournament only, verifies 200 with empty array
8. Write test "should return 404 for non-existent tournament" - verifies error handling

IMPORTANT: Use Next.js route handler pattern - call the route function directly with NextRequest, not HTTP calls.
Use { params: Promise.resolve({ id: tournament.id }) } pattern for async params per RESEARCH.md.
  </action>
  <verify>npm run docker:test -- --filter=web --testNamePattern="registrations"</verify>
  <done>Test file exists with 3+ tests, all pass with Testcontainers</done>
</task>

<task type="auto">
  <name>Task 2: Create matches API integration test</name>
  <files>apps/web/app/api/tournaments/[id]/matches/route.test.ts</files>
  <action>
Create integration test for GET /api/tournaments/[id]/matches using Testcontainers:
1. Import setupTestDatabase, clearTestDatabase, teardownTestDatabase from @fightrise/database/src/__tests__/setup
2. Import createUser, createTournament, createEvent, createMatch from seeders
3. Create beforeAll/beforeEach/afterAll hooks (same pattern as Task 1)
4. Write test "should return matches for tournament" - creates user + tournament + event + match, calls route, verifies 200
5. Write test "should return empty array when no matches" - creates tournament + event only
6. Write test "should filter matches by state" - creates matches with different states, tests filtering

Use the same Testcontainers pattern from Task 1.
  </action>
  <verify>npm run docker:test -- --filter=web --testNamePattern="matches"</verify>
  <done>Test file exists with 3+ tests, all pass with Testcontainers</done>
</task>

</tasks>

<verification>
All tests pass: npm run docker:test -- --filter=web
Tests use real PostgreSQL via Testcontainers
Tests cover success and error paths
</verification>

<success_criteria>
- API-01: Tournament registrations endpoint integration tests pass
- API-02: Tournament matches endpoint integration tests pass
- Both success and error paths covered
</success_criteria>

<output>
After completion, create `.planning/phases/06-web-api-startgg-integration-tests/06-01-SUMMARY.md`
</output>
