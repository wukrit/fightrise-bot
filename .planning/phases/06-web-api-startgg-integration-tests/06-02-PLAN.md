---
phase: 06-web-api-startgg-integration-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/api/matches/[id]/report/route.test.ts
  - apps/web/app/api/matches/[id]/dispute/route.test.ts
  - apps/web/app/api/matches/[id]/dq/route.test.ts
autonomous: true
requirements:
  - API-03
  - API-04
  - API-05
must_haves:
  truths:
    - "Match report endpoint handles score reporting correctly with Testcontainers"
    - "Match dispute endpoint handles dispute creation correctly"
    - "Match DQ endpoint handles disqualification correctly"
    - "All endpoints handle error cases properly"
  artifacts:
    - path: "apps/web/app/api/matches/[id]/report/route.test.ts"
      provides: "Integration tests for match score reporting"
      min_lines: 60
    - path: "apps/web/app/api/matches/[id]/dispute/route.test.ts"
      provides: "Integration tests for match disputes"
      min_lines: 40
    - path: "apps/web/app/api/matches/[id]/dq/route.test.ts"
      provides: "Integration tests for match DQ"
      min_lines: 40
  key_links:
    - from: "apps/web/app/api/matches/[id]/report/route.test.ts"
      to: "@fightrise/database/src/__tests__/setup"
      via: "setupTestDatabase() import"
      pattern: "setupTestDatabase.*clearTestDatabase"
    - from: "apps/web/app/api/matches/[id]/report/route.ts"
      to: "prisma.match"
      via: "Prisma transaction for atomic updates"
      pattern: "\\$transaction"
---

<objective>
Create integration tests for match API routes (report, dispute, dq endpoints) using Testcontainers for real database.

Purpose: Verify match-related API routes work correctly with a real PostgreSQL database, testing both success and error paths including race condition handling.

Output: Three test files in apps/web/app/api/matches/[id]/
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-web-api-startgg-integration-tests/06-CONTEXT.md
@.planning/phases/06-web-api-startgg-integration-tests/06-RESEARCH.md

# Key infrastructure from codebase:
- Testcontainers setup: packages/database/src/__tests__/setup.ts
- Seeders: packages/database/src/__tests__/utils/seeders.ts
- Existing API routes:
  - apps/web/app/api/matches/[id]/report/route.ts
  - apps/web/app/api/matches/[id]/dispute/route.ts
  - apps/web/app/api/matches/[id]/dq/route.ts
- Existing test (rewrite): apps/web/app/api/matches/[id]/report/route.test.ts (currently uses vi.mock)

# Test pattern to follow:
- Import setupTestDatabase, clearTestDatabase, teardownTestDatabase from database package
- Use factory functions from seeders
- Use Testcontainers for database isolation
- Test both success and error responses per CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite report API test to use Testcontainers</name>
  <files>apps/web/app/api/matches/[id]/report/route.test.ts</files>
  <action>
Rewrite the existing report API test to use Testcontainers instead of vi.mock:
1. Import setupTestDatabase, clearTestDatabase, teardownTestDatabase from @fightrise/database/src/__tests__/setup
2. Import createUser, createTournament, createMatch, createMatchPlayer from seeders
3. Replace vi.mock approach with real database setup
4. Create beforeAll that spins up Testcontainers PostgreSQL
5. Create beforeEach that clears database for isolation
6. Write tests:
   - "should report score successfully" - creates match in IN_PROGRESS state, reports score, verifies state transition to PENDING_CONFIRMATION
   - "should handle race condition when both players report" - tests atomic updateMany with state guard
   - "should return 400 for invalid score" - tests validation error
   - "should return 404 for non-existent match" - tests not found
   - "should return 409 for completed match" - tests state conflict
7. Keep afterAll that tears down container

IMPORTANT: The existing test uses vi.mock for race condition testing. Rewrite it to use real database with Testcontainers, using updateMany with state guards to test race condition prevention.
  </action>
  <verify>npm run docker:test -- --filter=web --testNamePattern="report"</verify>
  <done>Test rewritten with Testcontainers, 5+ tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create dispute API integration test</name>
  <files>apps/web/app/api/matches/[id]/dispute/route.test.ts</files>
  <action>
Create integration test for POST /api/matches/[id]/dispute:
1. Import setupTestDatabase, clearTestDatabase, teardownTestDatabase from database package
2. Import createUser, createTournament, createMatch, createMatchPlayer from seeders
3. Create beforeAll/beforeEach/afterAll hooks with Testcontainers
4. Write tests:
   - "should create dispute successfully" - creates match, calls dispute endpoint, verifies 201 with dispute data
   - "should not allow duplicate disputes" - tests idempotency
   - "should return 404 for non-existent match"
   - "should return 400 for invalid reason" - tests validation
   - "should return 409 if match already completed" - tests state validation
5. Verify the dispute creates an AuditLog entry

If the dispute route doesn't exist yet, create the route file at apps/web/app/api/matches/[id]/dispute/route.ts implementing POST endpoint.
  </action>
  <verify>npm run docker:test -- --filter=web --testNamePattern="dispute"</verify>
  <done>Test file exists with 4+ tests, all pass with Testcontainers</done>
</task>

<task type="auto">
  <name>Task 3: Create DQ API integration test</name>
  <files>apps/web/app/api/matches/[id]/dq/route.test.ts</files>
<action>
Create integration test for POST /api/matches/[id]/dq:
1. Import setupTestDatabase, clearTestDatabase, teardownTestDatabase from database package
2. Import createUser, createTournament, createMatch, createMatchPlayer from seeders
3. Create beforeAll/beforeEach/afterAll hooks with Testcontainers
4. Write tests:
   - "should DQ player successfully" - creates match, calls DQ endpoint, verifies player is DQ'd
   - "should return 404 for non-existent match"
   - "should return 403 if not tournament admin" - tests authorization
   - "should return 409 if match already completed" - tests state validation
   - "should handle opponent auto-win" - verifies match completion after DQ

If the DQ route doesn't exist yet, create the route file at apps/web/app/api/matches/[id]/dq/route.ts implementing POST endpoint that calls dqService.
  </action>
  <verify>npm run docker:test -- --filter=web --testNamePattern="dq"</verify>
  <done>Test file exists with 4+ tests, all pass with Testcontainers</done>
</task>

</tasks>

<verification>
All tests pass: npm run docker:test -- --filter=web
Tests use real PostgreSQL via Testcontainers
Tests cover success and error paths
</verification>

<success_criteria>
- API-03: Match report endpoint integration tests pass (rewritten with Testcontainers)
- API-04: Match dispute endpoint integration tests pass
- API-05: Match DQ endpoint integration tests pass
- All endpoints cover success and error paths
</success_criteria>

<output>
After completion, create `.planning/phases/06-web-api-startgg-integration-tests/06-02-SUMMARY.md`
</output>
