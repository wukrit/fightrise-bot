---
phase: 06-web-api-startgg-integration-tests
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/startgg-client/src/__tests__/integration/queries.test.ts
  - packages/startgg-client/src/__tests__/integration/mutations.test.ts
autonomous: true
requirements:
  - SGG-01
  - SGG-02
must_haves:
  truths:
    - "Start.gg GraphQL queries return correct data with MSW mocking"
    - "Start.gg GraphQL mutations work correctly with MSW mocking"
    - "Both success and error responses are tested"
  artifacts:
    - path: "packages/startgg-client/src/__tests__/integration/queries.test.ts"
      provides: "Integration tests for Start.gg GraphQL queries"
      min_lines: 80
    - path: "packages/startgg-client/src/__tests__/integration/mutations.test.ts"
      provides: "Integration tests for Start.gg GraphQL mutations"
      min_lines: 60
  key_links:
    - from: "packages/startgg-client/src/__tests__/integration/queries.test.ts"
      to: "packages/startgg-client/src/__mocks__/server"
      via: "MSW server setup"
      pattern: "server\\.listen.*server\\.close"
    - from: "packages/startgg-client/src/__tests__/integration/queries.test.ts"
      to: "packages/startgg-client/src/__mocks__/handlers"
      via: "GraphQL handlers import"
      pattern: "import.*handlers.*from"
---

<objective>
Create integration tests for Start.gg GraphQL client (queries and mutations) using MSW to mock the external API.

Purpose: Verify GraphQL query and mutation functions work correctly with mocked responses, testing both success and error paths.

Output: Two test files in packages/startgg-client/src/__tests__/integration/
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-web-api-startgg-integration-tests/06-CONTEXT.md
@.planning/phases/06-web-api-startgg-integration-tests/06-RESEARCH.md

# Key infrastructure from codebase:
- MSW server: packages/startgg-client/src/__mocks__/server.ts
- MSW handlers: packages/startgg-client/src/__mocks__/handlers.ts
- MSW fixtures: packages/startgg-client/src/__mocks__/fixtures.ts
- Start.gg client: packages/startgg-client/src/client.ts

# Existing handlers that can be reused:
- Success handlers for tournament queries
- Error handlers for not found, unauthorized, rate limit
- Mutation handlers for score reporting

# Test pattern to follow per RESEARCH.md:
- Import server from __mocks__/server
- Import handlers and errorHandlers from __mocks__/handlers
- Import fixtures from __mocks__/fixtures
- Use beforeAll/server.listen, afterEach/server.resetHandlers, afterAll/server.close lifecycle
- Test both success and error responses
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Start.gg GraphQL query integration tests</name>
  <files>packages/startgg-client/src/__tests__/integration/queries.test.ts</files>
  <action>
Create integration tests for Start.gg GraphQL queries using MSW:
1. Create directory: packages/startgg-client/src/__tests__/integration/
2. Import server from ../../__mocks__/server
3. Import handlers, errorHandlers from ../../__mocks__/handlers
4. Import mockTournaments, mockEvents, mockSets from ../../__mocks__/fixtures
5. Import client functions: getTournament, getEventSets, getEventEntrants from ../../client
6. Set up test lifecycle:
   - beforeAll: server.listen({ onUnhandledRequest: 'error' })
   - afterEach: server.resetHandlers()
   - afterAll: server.close()
7. Write tests for getTournament:
   - "should return tournament data on success" - use success handler, verify data matches fixture
   - "should return null for non-existent tournament" - use notFound handler
   - "should throw on invalid API key" - use unauthorized handler
   - "should throw on rate limit" - use rateLimited handler
8. Write tests for getEventSets:
   - "should return sets for event" - use sets handler
   - "should handle empty sets" - use emptySets handler
9. Write tests for getEventEntrants:
   - "should return entrants for event" - use entrants handler
   - "should handle pagination" - test page parameter
10. Ensure tests cover both success and error paths per CONTEXT.md

IMPORTANT: Use static fixtures from fixtures.ts, not dynamically generated data.
  </action>
  <verify>npm run docker:test -- --filter=startgg-client --testNamePattern="queries"</verify>
  <done>Test file exists with 8+ tests, all pass with MSW mocking</done>
</task>

<task type="auto">
  <name>Task 2: Create Start.gg GraphQL mutation integration tests</name>
  <files>packages/startgg-client/src/__tests__/integration/mutations.test.ts</files>
  <action>
Create integration tests for Start.gg GraphQL mutations using MSW:
1. Create file: packages/startgg-client/src/__tests__/integration/mutations.test.ts
2. Import server from ../../__mocks__/server
3. Import handlers, errorHandlers from ../../__mocks__/handlers
4. Import client mutation functions from ../../client
5. Set up test lifecycle (same pattern as queries):
   - beforeAll: server.listen({ onUnhandledRequest: 'error' })
   - afterEach: server.resetHandlers()
   - afterAll: server.close()
6. Write tests for reportScore mutation:
   - "should report score successfully" - use success handler, verify returned data
   - "should throw on invalid set ID" - use notFound handler
   - "should throw on unauthorized" - use unauthorized handler
   - "should throw on rate limit with retry" - use rateLimited handler
7. Write tests for other mutations if available (check client.ts for available mutations):
   - confirmScore mutation
   - disputeMatch mutation
   - createRegistration mutation (if exists)
8. Ensure error handlers cover: not found, unauthorized, rate limit, validation errors

IMPORTANT: Check client.ts for actual mutation method names and signatures.
  </action>
  <verify>npm run docker:test -- --filter=startgg-client --testNamePattern="mutations"</verify>
  <done>Test file exists with 6+ tests, all pass with MSW mocking</done>
</task>

</tasks>

<verification>
All tests pass: npm run docker:test -- --filter=startgg-client
Tests use MSW for GraphQL mocking
Tests cover success and error paths
</verification>

<success_criteria>
- SGG-01: GraphQL query integration tests pass with MSW mocking
- SGG-02: GraphQL mutation integration tests pass with MSW mocking
- Both success and error paths covered for each query/mutation
</success_criteria>

<output>
After completion, create `.planning/phases/06-web-api-startgg-integration-tests/06-03-SUMMARY.md`
</output>
