---
phase: 06-web-api-startgg-integration-tests
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/startgg-client/src/__tests__/integration/retry.test.ts
  - apps/web/app/api/tournaments/[id]/admin/audit/route.test.ts
autonomous: true
requirements:
  - SGG-03
  - API-06
must_haves:
  truths:
    - "Start.gg client retry logic works correctly with configurable options"
    - "Error handling covers rate limits, network errors, and timeouts"
    - "Audit log API endpoint works with Testcontainers"
  artifacts:
    - path: "packages/startgg-client/src/__tests__/integration/retry.test.ts"
      provides: "Integration tests for client retry logic and error handling"
      min_lines: 60
    - path: "apps/web/app/api/tournaments/[id]/admin/audit/route.test.ts"
      provides: "Integration tests for admin audit log API (rewritten with Testcontainers)"
      min_lines: 50
  key_links:
    - from: "packages/startgg-client/src/__tests__/integration/retry.test.ts"
      to: "packages/startgg-client/src/retry.ts"
      via: "withRetry function import"
      pattern: "withRetry.*import"
    - from: "apps/web/app/api/tournaments/[id]/admin/audit/route.test.ts"
      to: "@fightrise/database/src/__tests__/setup"
      via: "Testcontainers setup"
      pattern: "setupTestDatabase.*clearTestDatabase"
---

<objective>
Create integration tests for Start.gg client retry logic and rewrite audit API test to use Testcontainers.

Purpose: Verify retry logic and error handling work correctly, and update existing audit API test to use real database.

Output: Retry test file and rewritten audit API test
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-web-api-startgg-integration-tests/06-CONTEXT.md
@.planning/phases/06-web-api-startgg-integration-tests/06-RESEARCH.md

# Key infrastructure from codebase:
- Retry logic: packages/startgg-client/src/retry.ts
- Testcontainers setup: packages/database/src/__tests__/setup.ts
- Seeders: packages/database/src/__tests__/utils/seeders.ts
- Existing audit test: apps/web/app/api/tournaments/[id]/admin/audit/route.test.ts

# Test pattern for retry:
- Import withRetry from retry.ts
- Mock functions that fail/ucceed to test retry behavior
- Test rate limit errors trigger retry
- Test network errors trigger retry
- Test non-retryable errors (validation, auth) don't retry

# Test pattern for audit API:
- Rewrite existing test to use Testcontainers
- Keep same test coverage but with real database
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create retry logic integration tests</name>
  <files>packages/startgg-client/src/__tests__/integration/retry.test.ts</files>
  <action>
Create integration tests for the Start.gg client retry logic:
1. Create file: packages/startgg-client/src/__tests__/integration/retry.test.ts
2. Import withRetry from ../../retry
3. Write tests for retry behavior:
   - "should succeed on first attempt" - mock function succeeds, verify no retries
   - "should retry on rate limit error (429)" - mock fails with 429, succeeds on retry, verify 2 calls
   - "should retry on network error (ECONNREFUSED)" - mock fails with network error, succeeds on retry
   - "should retry on timeout (ETIMEDOUT)" - mock fails with timeout, succeeds on retry
   - "should not retry on validation error (4xx except 429)" - mock fails with 400, verify throws immediately
   - "should not retry on auth error (401/403)" - mock fails with 401, verify throws immediately
   - "should respect maxRetries config" - mock always fails, verify exactly maxRetries+1 attempts
   - "should use exponential backoff" - mock fails twice, verify delay increases
   - "should log retry attempts" - verify console.log or logger called on retry
4. Test with different retry configurations:
   - Default config (3 retries, exponential backoff)
   - Custom maxRetries: 0 (no retries)
   - Custom maxRetries: 5 (more retries)
5. Test error types:
   - RateLimitError
   - NetworkError
   - TimeoutError
   - ValidationError

IMPORTANT: These are UNIT tests of the retry logic, not integration tests with MSW.
Use vi.fn() to mock functions that fail/succeed.
  </action>
  <verify>npm run docker:test -- --filter=startgg-client --testNamePattern="retry"</verify>
  <done>Test file exists with 10+ tests, all pass</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite audit API test to use Testcontainers</name>
  <files>apps/web/app/api/tournaments/[id]/admin/audit/route.test.ts</files>
  <action>
Rewrite the existing audit API test to use Testcontainers instead of vi.mock:
1. Read existing test to understand coverage
2. Import setupTestDatabase, clearTestDatabase, teardownTestDatabase from database package
3. Import createUser, createTournament, createAuditLog from seeders
4. Replace vi.mock approach with Testcontainers setup
5. Keep same test coverage but with real database:
   - "should return audit logs for tournament"
   - "should filter by action type"
   - "should paginate results"
   - "should return empty for unauthorized user"
   - "should return 404 for non-existent tournament"
6. Update beforeAll to spin up Testcontainers PostgreSQL
7. Update beforeEach to clear database for isolation
8. Update afterAll to tear down container

IMPORTANT: Keep the same test assertions but use real database operations instead of mocks.
  </action>
  <verify>npm run docker:test -- --filter=web --testNamePattern="audit"</verify>
  <done>Test rewritten with Testcontainers, all tests pass</done>
</task>

</tasks>

<verification>
All tests pass: npm run docker:test
Retry tests: 10+ tests pass
Audit API tests: All tests pass with Testcontainers
</verification>

<success_criteria>
- SGG-03: Client retry logic and error handling tests pass
- API-06: Admin audit log API integration tests pass (rewritten with Testcontainers)
- All error handling scenarios covered
</success_criteria>

<output>
After completion, create `.planning/phases/06-web-api-startgg-integration-tests/06-04-SUMMARY.md`
</output>
