---
phase: 05-bot-services-unit-tests
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified: []
autonomous: true
requirements: [ANALYSIS-01, ANALYSIS-02, ANALYSIS-03, BOT-01, BOT-04, BOT-06, BOT-07, SHR-01, SHR-02]
user_setup: []

must_haves:
  truths:
    - "All bot service tests run and pass"
    - "Coverage report shows 80%+ for bot services"
    - "Test patterns documented for consistency"
    - "Coverage gaps identified and documented"
  artifacts:
    - path: ".planning/phases/05-bot-services-unit-tests/COVERAGE.md"
      provides: "Test coverage analysis and gaps documentation"
    - path: ".planning/phases/05-bot-services-unit-tests/TEST_PATTERNS.md"
      provides: "Documented test patterns for consistency"
  key_links:
    - from: "COVERAGE.md"
      to: "All bot service test files"
      via: "Coverage analysis"
---

<objective>
Run all existing bot service tests and shared utility tests, analyze coverage, document test patterns, and identify any remaining gaps. This completes the analysis requirements (ANALYSIS-01, ANALYSIS-02, ANALYSIS-03) and verifies all existing tests (BOT-01, BOT-04, BOT-06, BOT-07, SHR-01, SHR-02) still pass.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-bot-services-unit-tests/05-CONTEXT.md

# Existing Tests (from 05-RESEARCH.md)
| Service | Test File | Status |
|---------|-----------|--------|
| matchService | apps/bot/src/services/__tests__/matchService.test.ts | Comprehensive - 40+ tests |
| tournamentService | apps/bot/src/services/__tests__/tournamentService.test.ts | Partial |
| registrationSyncService | apps/bot/src/services/__tests__/registrationSyncService.test.ts | Partial |
| pollingService | apps/bot/src/__tests__/services/pollingService.test.ts | Partial |
| validation | packages/shared/src/validation.test.ts | Complete |
| errors | packages/shared/src/errors.test.ts | Complete |
| datetime | packages/shared/src/datetime.test.ts | Complete |
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all bot service tests</name>
  <files>apps/bot/src/services/__tests__/*.test.ts</files>
  <action>
Run all bot service unit tests:
```bash
cd apps/bot && npm run test
```

Also run shared package tests:
```bash
cd packages/shared && npm run test
```

Verify all tests pass. This covers the existing tests for:
- BOT-01: matchService
- BOT-04: tournamentService
- BOT-06: registrationSyncService
- BOT-07: pollingService
- SHR-01: validation schemas
- SHR-02: error classes
  </action>
  <verify>cd apps/bot && npm run test && cd ../../packages/shared && npm run test</verify>
  <done>All existing bot service and shared utility tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Generate coverage report</name>
  <files>apps/bot/src/services/__tests__</files>
  <action>
Generate coverage report to analyze current test coverage:
```bash
cd apps/bot && npm run test -- --coverage
```

Note: If coverage config is not set up, create/update vitest.config.ts to include coverage:
```typescript
export default defineConfig({
  test: {
    coverage: {
      provider: 'v8',
      reporter: ['text', 'json', 'html'],
      include: ['src/**/*.ts'],
      exclude: ['src/**/*.d.ts', 'src/**/__tests__/**', 'src/index.ts'],
    },
  },
});
```

Analyze the coverage report and document:
- Line coverage percentage
- Branch coverage percentage
- Files with good coverage
- Files with gaps
- Recommendations for improvement
  </action>
  <verify>Coverage report generated in apps/bot/coverage/</verify>
  <done>Coverage report available with line and branch percentages</done>
</task>

<task type="auto">
  <name>Task 3: Document test patterns</name>
  <files>.planning/phases/05-bot-services-unit-tests/TEST_PATTERNS.md</files>
  <action>
Create TEST_PATTERNS.md documenting the established test patterns for the project:

```markdown
# Test Patterns - FightRise Bot

## Test Structure
- Location: `__tests__/` folders within service/handler directories
- Naming: `*.test.ts` suffix
- Setup: Shared `beforeEach` with vi.clearAllMocks()

## DiscordTestClient Pattern
Use for testing button handlers and slash commands:
```typescript
import { createDiscordTestClient } from '../../__tests__/harness';

const client = createDiscordTestClient({ userId: 'user-123' });
const interaction = await client.clickButton('checkin:match123:1');
expect(interaction.lastReply?.content).toBe('Expected response');
```

## vi.mock Pattern
Mock external dependencies:
```typescript
vi.mock('@fightrise/database', () => ({
  prisma: { ... },
  MatchState: { ... },
}));
```

## Prisma Transaction Mock
Use for services using transactions:
```typescript
vi.mock('@fightrise/database', () => ({
  prisma: {
    $transaction: vi.fn((cb) => cb(prisma)),
    // ...
  },
}));
```

## Test Scope
- Happy paths: Test successful scenarios
- Error paths: Test failure modes
- Edge cases: null, undefined, empty, invalid input
```
  </action>
  <verify>TEST_PATTERNS.md created with documented patterns</verify>
  <done>Test patterns documented for future consistency</done>
</task>

<task type="auto">
  <name>Task 4: Document coverage gaps</name>
  <files>.planning/phases/05-bot-services-unit-tests/COVERAGE.md</files>
  <action>
Create COVERAGE.md documenting coverage analysis:

```markdown
# Test Coverage Analysis - Phase 5

## Summary
- Total bot service tests: X tests
- Overall line coverage: X%
- Overall branch coverage: X%

## By Service

| Service | Line Coverage | Status |
|---------|---------------|--------|
| matchService | ~90% | Good |
| tournamentService | ~60% | Needs work |
| registrationSyncService | ~50% | Needs work |
| pollingService | ~70% | Moderate |
| dqService | NEW | Good |
| scoreHandler | NEW | Good |
| checkinHandler | NEW | Good |

## Gaps Identified
1. [List specific gaps with file:line references]

## Recommendations
1. [Actionable recommendations for closing gaps]
```
  </action>
  <verify>COVERAGE.md created with analysis</verify>
  <done>Coverage gaps documented with recommendations</done>
</task>

</tasks>

<verification>
All tasks complete:
- All tests pass
- Coverage report generated
- TEST_PATTERNS.md created
- COVERAGE.md created
</verification>

<success_criteria>
- All bot service tests pass (BOT-01, BOT-04, BOT-06, BOT-07)
- All shared utility tests pass (SHR-01, SHR-02)
- Analysis complete with documentation (ANALYSIS-01, ANALYSIS-02, ANALYSIS-03)
- Coverage gaps identified
</success_criteria>

<output>
After completion, create `.planning/phases/05-bot-services-unit-tests/05-03-SUMMARY.md`
</output>
