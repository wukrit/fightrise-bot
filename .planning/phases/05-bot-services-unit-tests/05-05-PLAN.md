---
phase: 05-bot-services-unit-tests
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/bot/src/services/__tests__/registrationSyncService.test.ts
autonomous: true
requirements: [BOT-06]
user_setup: []

must_haves:
  truths:
    - "notifyNewRegistrations tests pass - Discord notifications work correctly"
    - "Additional edge case tests for syncEventRegistrations"
    - "registrationSyncService achieves 80%+ code coverage"
  artifacts:
    - path: "apps/bot/src/services/__tests__/registrationSyncService.test.ts"
      provides: "Tests for notifyNewRegistrations and edge cases"
      min_lines: 100
  key_links:
    - from: "apps/bot/src/services/__tests__/registrationSyncService.test.ts"
      to: "apps/bot/src/services/registrationSyncService.ts"
      via: "vi.mock for StartGGClient + Prisma"
---

<objective>
Expand registrationSyncService tests to achieve 80%+ code coverage. Test the notifyNewRegistrations function and additional edge cases for syncEventRegistrations.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-bot-services-unit-tests/05-CONTEXT.md
@.planning/phases/05-bot-services-unit-tests/05-RESEARCH.md
@.planning/phases/05-bot-services-unit-tests/TEST_PATTERNS.md

# Current registrationSyncService test status
# Tests exist in apps/bot/src/services/__tests__/registrationSyncService.test.ts
# Already tested: syncEventRegistrations (multiple scenarios)
# Needs: notifyNewRegistrations, more edge cases
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add notifyNewRegistrations tests</name>
  <files>apps/bot/src/services/__tests__/registrationSyncService.test.ts</files>
  <action>
Add comprehensive tests for notifyNewRegistrations function:

1. Test sends Discord message when new registrations added
2. Test does not send message when no new registrations
3. Test handles missing Discord client gracefully
4. Test handles Discord DM fails gracefully (catches error)

Use vi.mock to mock:
- Discord.js Client and TextChannel
- Prisma (existing pattern from existing tests)

The function should:
- Take discord client as parameter
- Send DM to each new user
- Return after processing (no specific return value expected)
  </action>
  <verify>Run: npm run docker:test -- --filter=@fightrise/bot -- --run apps/bot/src/services/__tests__/registrationSyncService.test.ts</verify>
  <done>notifyNewRegistrations has 4+ passing tests covering success and error paths</done>
</task>

<task type="auto">
  <name>Task 2: Add additional edge case tests</name>
  <files>apps/bot/src/services/__tests__/registrationSyncService.test.ts</files>
  <action>
Add additional edge case tests for syncEventRegistrations:

1. Test handles event not found in database
2. Test handles user with only partial startgg data (no gamerTag)
3. Test handles entrant with no participants
4. Test handles multiple participants per entrant (takes first)
5. Test does not overwrite confirmed status with pending
6. Test handles database transaction errors gracefully

These test the remaining branches in computeBatchOperations and validateEntrant.
  </action>
  <verify>Run: npm run docker:test -- --filter=@fightrise/bot -- --run apps/bot/src/services/__tests__/registrationSyncService.test.ts</verify>
  <done>5+ additional edge case tests added and passing</done>
</task>

<task type="auto">
  <name>Task 3: Verify 80% coverage target</name>
  <files>apps/bot/src/services/__tests__/registrationSyncService.test.ts</files>
  <action>
Run coverage report to verify 80%+ line coverage for registrationSyncService.ts:
1. Run: npm run docker:test -- --filter=@fightrise/bot -- --coverage --run apps/bot/src/services/__tests__/registrationSyncService.test.ts
2. Check coverage report for registrationSyncService.ts
3. If below 80%, add more edge case tests

Focus on uncovered lines:
- notifyNewRegistrations function
- validateEntrant edge cases
- Error handling in fetchEntrantsFromStartgg
  </action>
  <verify>npm run docker:test -- --filter=@fightrise/bot -- --coverage --run apps/bot/src/services/__tests__/registrationSyncService.test.ts | grep -A5 "registrationSyncService"</verify>
  <done>registrationSyncService.ts achieves 80%+ line coverage</done>
</task>

</tasks>

<verification>
Run the full bot test suite to ensure no regressions:
npm run docker:test -- --filter=@fightrise/bot -- --run
</verification>

<success_criteria>
1. notifyNewRegistrations has 4+ passing tests
2. 5+ additional edge case tests added
3. Total registrationSyncService tests achieve 80%+ code coverage
4. All bot unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-bot-services-unit-tests/05-05-SUMMARY.md`
</output>
