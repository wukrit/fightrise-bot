---
phase: 05-bot-services-unit-tests
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/bot/src/__tests__/services/pollingService.test.ts
autonomous: true
requirements: [BOT-07]
user_setup: []

must_haves:
  truths:
    - "startPollingService tests pass with mocked Redis and BullMQ"
    - "stopPollingService tests pass - closes queue and worker correctly"
    - "schedulePoll tests pass - adds jobs to queue with correct delay"
    - "pollingService achieves 80%+ code coverage"
  artifacts:
    - path: "apps/bot/src/__tests__/services/pollingService.test.ts"
      provides: "Tests for startPollingService, stopPollingService, schedulePoll"
      min_lines: 150
  key_links:
    - from: "apps/bot/src/__tests__/services/pollingService.test.ts"
      to: "apps/bot/src/services/pollingService.ts"
      via: "vi.mock for BullMQ + Redis"
---

<objective>
Expand pollingService tests to achieve 80%+ code coverage. Test the core service functions: startPollingService, stopPollingService, and schedulePoll.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-bot-services-unit-tests/05-CONTEXT.md
@.planning/phases/05-bot-services-unit-tests/05-RESEARCH.md
@.planning/phases/05-bot-services-unit-tests/TEST_PATTERNS.md

# Current pollingService test status
# Tests exist in apps/bot/src/__tests__/services/pollingService.test.ts
# Already tested: calculatePollInterval, getPollStatus, triggerImmediatePoll
# Needs: startPollingService, stopPollingService, schedulePoll
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add startPollingService tests</name>
  <files>apps/bot/src/__tests__/services/pollingService.test.ts</files>
  <action>
Add comprehensive tests for startPollingService function:
1. Test successful startup with all dependencies (Redis, BullMQ queue, worker)
2. Test error when STARTGG_API_KEY is missing
3. Test error when Redis connection is unavailable
4. Test queue and worker are created with correct options
5. Test scheduleActiveTournaments is called on startup
6. Test Discord client is stored when provided
7. Test warning logged when no Discord client provided

Use vi.mock to mock:
- BullMQ (Queue, Worker)
- Redis connection (getRedisConnection)
- The registrationSyncService
- The matchService (createMatchThread)
- Discord.js Client

Follow the existing test patterns in the file.
  </action>
  <verify>Run: npm run docker:test -- --filter=@fightrise/bot -- --run apps/bot/src/__tests__/services/pollingService.test.ts</verify>
  <done>startPollingService has 5+ passing tests covering success and error paths</done>
</task>

<task type="auto">
  <name>Task 2: Add stopPollingService and schedulePoll tests</name>
  <files>apps/bot/src/__tests__/services/pollingService.test.ts</files>
  <action>
Add tests for stopPollingService and schedulePoll:

For stopPollingService:
1. Test closes queue and worker when both exist
2. Test handles missing queue gracefully
3. Test handles missing worker gracefully

For schedulePoll:
1. Test adds job to queue with correct tournamentId and delay
2. Test returns early when queue is null (service not started)
3. Test uses jobId to prevent duplicate jobs

Use the same mocking approach as startPollingService tests.
  </action>
  <verify>Run: npm run docker:test -- --filter=@fightrise/bot -- --run apps/bot/src/__tests__/services/pollingService.test.ts</verify>
  <done>stopPollingService has 3+ tests, schedulePoll has 3+ tests - all passing</done>
</task>

<task type="auto">
  <name>Task 3: Verify 80% coverage target</name>
  <files>apps/bot/src/__tests__/services/pollingService.test.ts</files>
  <action>
Run coverage report to verify 80%+ line coverage for pollingService.ts:
1. Run: npm run docker:test -- --filter=@fightrise/bot -- --coverage --run apps/bot/src/__tests__/services/pollingService.test.ts
2. Check coverage report for pollingService.ts
3. If below 80%, add more edge case tests

Focus on uncovered lines:
- Error handling paths
- Edge cases in match state transitions
- Null/undefined handling
  </action>
  <verify>npm run docker:test -- --filter=@fightrise/bot -- --coverage --run apps/bot/src/__tests__/services/pollingService.test.ts | grep -A5 "pollingService"</verify>
  <done>pollingService.ts achieves 80%+ line coverage</done>
</task>

</tasks>

<verification>
Run the full bot test suite to ensure no regressions:
npm run docker:test -- --filter=@fightrise/bot -- --run
</verification>

<success_criteria>
1. startPollingService has 5+ passing tests
2. stopPollingService has 3+ passing tests
3. schedulePoll has 3+ passing tests
4. Total pollingService tests achieve 80%+ code coverage
5. All bot unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-bot-services-unit-tests/05-04-SUMMARY.md`
</output>
