{
  "name": "@fightrise/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "[ -f ../../.env ] && dotenv -e ../../.env -- vitest run || vitest run",
    "test:smoke": "[ -f ../../.env ] && dotenv -e ../../.env -- vitest run --config vitest.smoke.config.ts || vitest run --config vitest.smoke.config.ts",
    "db:generate": "prisma generate",
    "db:push": "[ -f ../../.env ] && dotenv -e ../../.env -- prisma db push --accept-data-loss || prisma db push --accept-data-loss",
    "db:migrate": "[ -f ../../.env ] && dotenv -e ../../.env -- prisma migrate dev || prisma migrate dev",
    "db:studio": "dotenv -e ../../.env -- prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.7.0",
    "discord.js": "13.17.1",
    "next": "16.1.6",
    "vite": "7.3.1"
  },
  "devDependencies": {
    "@testcontainers/postgresql": "^10.0.0",
    "dotenv-cli": "^11.0.0",
    "prisma": "^5.7.0",
    "typescript": "^5.3.0",
    "vitest": "^4.0.18"
  }
}
