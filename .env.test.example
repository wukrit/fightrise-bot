# Test Environment Variables
# Copy this file to .env.test and fill in values for running tests

# ===========================================
# Unit & Integration Test Environment
# ===========================================

# Database (for integration tests)
# Testcontainers will override this with a temporary database
DATABASE_URL=postgresql://fightrise_test:testpassword@localhost:5433/fightrise_test

# Redis (for integration tests with BullMQ)
REDIS_URL=redis://localhost:6380

# Dummy values for tests that need env vars present but don't use them
DISCORD_TOKEN=test-discord-token
DISCORD_CLIENT_ID=test-client-id
DISCORD_CLIENT_SECRET=test-client-secret
STARTGG_API_KEY=test-startgg-api-key
NEXTAUTH_SECRET=test-nextauth-secret-at-least-32-chars
NEXTAUTH_URL=http://localhost:3000
AUTH_BYPASS_FOR_E2E=true

# ===========================================
# Smoke Test Credentials
# These are required ONLY for smoke tests
# DO NOT commit real values to version control
# ===========================================

# Discord Bot Smoke Tests
# Use a test bot in a dedicated test server
SMOKE_DISCORD_TOKEN=
SMOKE_DISCORD_GUILD_ID=
SMOKE_DISCORD_CHANNEL_ID=

# Start.gg API Smoke Tests
# Your personal Start.gg API key
SMOKE_STARTGG_API_KEY=
# A known public tournament slug for testing
SMOKE_STARTGG_TOURNAMENT_SLUG=tournament/evo-2023

# Discord OAuth Smoke Tests
# A separate Discord application for testing
SMOKE_DISCORD_CLIENT_ID=
SMOKE_DISCORD_CLIENT_SECRET=
SMOKE_OAUTH_REDIRECT_URI=http://localhost:3000/api/auth/callback/discord

# ===========================================
# CI/CD Secrets Configuration
# Set these as GitHub repository secrets:
#
# SMOKE_DISCORD_TOKEN - Test bot token
# SMOKE_DISCORD_GUILD_ID - Test server ID
# SMOKE_DISCORD_CHANNEL_ID - Test channel ID
# SMOKE_STARTGG_API_KEY - Start.gg API key
# SMOKE_DISCORD_CLIENT_ID - OAuth app client ID
# SMOKE_DISCORD_CLIENT_SECRET - OAuth app client secret
# SLACK_WEBHOOK_URL - (Optional) For failure notifications
#
# And as GitHub repository variables:
# SMOKE_STARTGG_TOURNAMENT_SLUG - Public tournament for testing
# ===========================================
